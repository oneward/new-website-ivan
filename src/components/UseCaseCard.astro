---
import DotMark from './DotMark.astro';
import Icon from './Icon.astro';
import { catShort, rolesText, type UseCase } from '../data/useCases';

// One card of the use-case grid. On the homepage it links to the library;
// in the library it opens the detail modal (data-uc-* feeds main.js).
interface Props {
  u: UseCase;
  href?: string;
  clickable?: boolean;
  moreLabel?: string;
}
const { u, href, clickable = false, moreLabel = 'Details →' } = Astro.props;
const Tag = href ? 'a' : 'div';
const detail = clickable
  ? {
      'data-uc-slug': u.slug,
      'data-uc-title': u.title,
      'data-uc-cat': catShort[u.cat] ?? u.cat,
      'data-uc-users': u.users,
      'data-uc-legacy': u.legacy,
      'data-uc-solution': u.solution,
      'data-uc-icon': u.icon,
      'data-uc-foundation': String(!!u.f),
      role: 'button',
      tabindex: 0,
    }
  : {};
---
<Tag
  class:list={['uc-card', { 'is-clickable': clickable || !!href }]}
  href={href}
  data-uc-roles={u.roles.join('|')}
  {...detail}
  style={`background: ${u.f ? '#f5f6ff' : '#ffffff'}; border: 1px solid ${u.f ? 'rgba(3,15,255,0.2)' : '#e3e1db'}; border-radius: 12px; padding: 20px 20px 18px; display: flex; flex-direction: column; gap: 14px; min-height: 100%; box-sizing: border-box;`}
>
  <slot name="preview" />
  <div style="display: flex; align-items: center; justify-content: space-between; gap: 10px;">
    <span style={`width: 36px; height: 36px; border-radius: 10px; background: ${u.f ? '#ffffff' : 'rgba(3,15,255,0.06)'}; display: flex; align-items: center; justify-content: center;`}><Icon src={`/assets/ph-${u.icon}.svg`} size={18} color="#030FFF" /></span>
    {u.f && <span style="display: flex; align-items: center; gap: 6px; font-size: 11px; color: #030FFF; background: #ffffff; border: 1px solid rgba(3,15,255,0.2); border-radius: 999px; padding: 4px 9px 4px 7px; white-space: nowrap;"><DotMark size={11} />Always on</span>}
  </div>
  <div style="display: flex; flex-direction: column; gap: 6px;">
    <span style={`font-family: 'IBM Plex Mono', monospace; font-size: 10px; letter-spacing: 0.14em; color: ${u.f ? '#030FFF' : '#9a968c'};`}>{catShort[u.cat] ?? u.cat}</span>
    <span style="font-family: 'Sora', sans-serif; font-size: 15px; font-weight: 500; letter-spacing: -0.01em; color: #101828;">{u.title}</span>
    <span style="font-size: 13px; line-height: 1.6; color: #6f6a5e; text-wrap: pretty;">{u.body}</span>
  </div>
  <div style={`margin-top: auto; padding-top: 12px; border-top: 1px solid ${u.f ? 'rgba(3,15,255,0.12)' : '#eceae4'}; display: flex; align-items: center; justify-content: space-between; gap: 10px;`}>
    <span style="display: flex; align-items: center; gap: 7px; min-width: 0;"><Icon src="/assets/00b81253.svg" size={13} color="#9a968c" /><span style="font-size: 12px; color: #6f6a5e;">{rolesText(u.roles)}</span></span>
    {(clickable || href) && <span class="uc-card-more" style="font-size: 12px; color: #030FFF; white-space: nowrap;">{moreLabel}</span>}
  </div>
</Tag>
